#
# Config file for tokenize.pm
#
# $output_dir (not required)
#    Specify a directory to write client detail
#    files. If not specified, defaults to current
#    directory.
#
# %stopwords (required)
#    A hash containing terms to ignore.
#

$output_dir   = "";
%stopwords = ( "com"   => "",
               "edu"   => "",
               "www"   => "",
               "org"   => ""
             );

1;
